# TODO
- [x] Extract embedded CSS from `index.html` into `style.css`
- [x] Extract embedded JS from `index.html` into `app.js`
- [x] Update `index.html` to load `style.css` and `app.js`
- [ ] Run a quick check by opening `index.html` in a browser to confirm UI still works

